server:
  forward-headers-strategy: NATIVE
spring:
  sql:
    init:
      mode: always
  cloud:
    gcp:
      spanner:
        instance-id: test-instance
        database: example-db
        project-id: test-project
        emulator:
          enabled: true
otel:
  sdk:
    disabled: true